---
description: Capture what we just learned/solved into the Mastermind brain as the right note type.
---

Capture the current learning into the Mastermind vault:
1. Decide the note type (`gotcha|decision|pattern|stack|howto`).
2. `search` first to avoid duplicates — prefer `edit_note` on an existing note.
3. `write_note` with full frontmatter per the mastermind-brain conventions.
4. Link it to the relevant `projects/<name>` hub and related notes.
5. Show me the created/updated note path.

Extra context/hint: $ARGUMENTS
