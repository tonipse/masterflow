---
description: Capture what we just learned or solved into the Mastermind brain as the right note type (gotcha, decision, pattern, howto, stack).
argument-hint: "[hint what to capture]"
---

Capture the current learning into the Mastermind vault. Extra hint from the user: "$ARGUMENTS"

1. Load the skill `mastermind:mastermind-brain` (Skill tool) and read its `conventions.md`; follow the
   capture procedure there exactly (quality bar §10, dedup §8, frontmatter §2, titles §3, links §6).
2. Pick the type: `gotcha | decision | pattern | howto | stack`. If the learning has two topics, write two notes.
3. `search_notes` first; prefer `edit_note` on an existing note over a near-duplicate.
4. `write_note` / `edit_note` with complete frontmatter, then add the backlink bullet to the project hub.
   If the project has no hub yet, create it first with the `mastermind-project` procedure.
5. Reply in German with one line per note: type, title, path. No questions.
