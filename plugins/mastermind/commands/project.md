---
description: Create or update a project hub note in the Mastermind brain.
---

Create or update the project hub note `projects/<name>.md` for: $ARGUMENTS
Use the `project` type and templates/project.md structure. If it exists, `edit_note` to update; else `write_note`. Link relevant patterns/gotchas/decisions/stacks into it, and link it to `[[index]]`. Show the note path.
