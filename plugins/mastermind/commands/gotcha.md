---
description: Quickly record a problem → cause → solution as a gotcha note in the Mastermind brain.
---

Record a gotcha in the Mastermind vault about: $ARGUMENTS
Use the `gotcha` type and the templates/gotcha.md structure (Problem / Symptom / Ursache / Lösung). `search_notes` first to avoid duplicates; capture the key fact as `- [gotcha] … #tag`; link to the relevant project hub and `[[index]]`. Show the note path.
